<Page
    x:Class="KinectEx.Demo.Playback.Store.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KinectEx.Demo.Playback.Store"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Rectangle Fill="Black" Grid.Row="0"/>
        <Image x:Name="OutputImage" Grid.Row="0"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button x:Name="OpenButton" Margin="5 0">Open</Button>
            <Button x:Name="PlayButton" Margin="5 0">Play</Button>
            <TextBlock FontSize="18" FontWeight="Bold" Margin="15 0 0 0" VerticalAlignment="Center">Output:</TextBlock>
            <ComboBox x:Name="OutputCombo" Margin="5"/>
            <TextBlock FontSize="18" FontWeight="Bold" Margin="15 0 0 0" VerticalAlignment="Center">Playback Location:</TextBlock>
            <Slider x:Name="LocationSlider" Margin="5 0" Width="500" VerticalAlignment="Center" Minimum="0" Maximum="100"/>
        </StackPanel>
    </Grid>
</Page>
